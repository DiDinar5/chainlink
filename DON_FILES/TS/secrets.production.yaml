# Upload with:
# cre secrets create ./secrets.production.yaml --target production-settings

secretsNames:
  SUMSUB_APP_TOKEN:
    - SUMSUB_APP_TOKEN_VALUE
  SUMSUB_SECRET_KEY:
    - SUMSUB_SECRET_KEY_VALUE
  # Optional (if workflow implementation reads these from DON secrets):
  # WORLD_ID_APP_ID:
  #   - WORLD_ID_APP_ID_VALUE
  # WORLD_ID_ACTION:
  #   - WORLD_ID_ACTION_VALUE
